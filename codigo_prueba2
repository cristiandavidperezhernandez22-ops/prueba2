def veces(veces:int)->str:
    """
    funciona para crear una lista
    en diferentes lineas de los
    numeros del 1 al indicado.
    """
    numeros = []
    for i in range(veces):
        numeros.append(i+1)
    return numeros

a = int(input("veces?: \n"))
print(veces(a))