def lista_hasta(max:int)->list:
    """
    funciona para crear una lista de
    los numeros del 1 al especificado.
    """
    numeros = []
    for i in range(max):
        numeros.append(i+1)
    return numeros

a = int(input("numero maximo?: \n"))
print(lista_hasta(a))
help(lista_hasta)